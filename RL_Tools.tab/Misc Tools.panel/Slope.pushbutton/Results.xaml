<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Slope Results"
        Height="620"
        Width="1180"
        MinHeight="520"
        MinWidth="980"
        WindowStartupLocation="CenterScreen"
        ShowInTaskbar="False"
        ResizeMode="CanResizeWithGrip">
    <Grid Margin="14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,10">
            <TextBlock Text="Slope Results" FontSize="30" FontWeight="SemiBold"/>
            <TextBlock Text="Skipped and failed elements are listed below."
                       Margin="0,6,0,0"
                       FontSize="13"
                       Foreground="DimGray"/>
        </StackPanel>

        <Border Grid.Row="1" BorderBrush="#D0D0D0" BorderThickness="1" Padding="10" Margin="0,0,0,10">
            <TextBlock x:Name="summary_tb" TextWrapping="Wrap" Foreground="DimGray"/>
        </Border>

        <ListView x:Name="result_lv"
                  Grid.Row="2"
                  BorderBrush="#B9B9B9"
                  BorderThickness="1">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Element Id" Width="110" DisplayMemberBinding="{Binding element_id}"/>
                    <GridViewColumn Header="Category" Width="170" DisplayMemberBinding="{Binding category_name}"/>
                    <GridViewColumn Header="Element Name" Width="250" DisplayMemberBinding="{Binding element_name}"/>
                    <GridViewColumn Header="Current Slope" Width="160" DisplayMemberBinding="{Binding current_slope}"/>
                    <GridViewColumn Header="Reason" Width="430" DisplayMemberBinding="{Binding reason}"/>
                </GridView>
            </ListView.View>
        </ListView>

        <Border Grid.Row="3" BorderBrush="#D0D0D0" BorderThickness="1" Padding="10" Margin="0,10,0,0">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Close" Width="120" Height="36" Click="close_click"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
